import React from 'react';
import BookingForm from '../components/BookingForm';

const Book = () => {
    return (
        <div className="book-page" style={{ paddingTop: '80px', minHeight: '80vh' }}>
            <BookingForm />
        </div>
    );
};

export default Book;
